import React from "react";
import Header from "./Header"; // Import the Header component if not already imported

const MyComponent = () => {
  return (
    <div>
      <Header />
      <p>Hello!</p>
    </div>
  );
};

export default MyComponent;
